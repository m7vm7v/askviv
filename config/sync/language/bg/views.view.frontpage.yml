display:
  default:
    display_options:
      fields:
        nothing:
          alter:
            text: "<a href=\"{{ path }}\" class=\"blog-image overlay-container\">\n{{ field_image }} \n<div class=\"image-gradient\"></div>\n</div>\n\n<div class='blog-info-box'>\n<h2>{{ title }}</h2>\n<p>{{ field_short_description }}</p>\n<div class=\"button button--primary\">{{ view_node }}</div>\n</a>"
        view_node:
          text: 'Научи повече'
      exposed_form:
        options:
          submit_button: Apply
          reset_button_label: Reset
  page_1:
    display_title: Начало
